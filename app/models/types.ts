export interface Types {
    id: number;
    name: string;
    description: string;
    isActive: boolean;
}

export interface typeActiveItem {
    id: string;
    isActive: boolean;

}
export interface setActiveType {
    id: string;
    isActive: boolean;

}